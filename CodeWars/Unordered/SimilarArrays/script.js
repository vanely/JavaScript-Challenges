//given two arrays. if 2 items in one of the arrays can be swap to equal the other array return true else return false.

let A = [1, 2, 3];
let B = [2, 3, 1];

console.log();
for(let i = 0; i < B.length; i++) {
    
    console.log(B[i]);
}


function areSimilar(A, B) {

    for(let i = 0; i < A.length; i++) {
        let indexOne = A[0];
        if(indexOne > A[i]) {
            A
        }

    }


    if(ASort === BSort)
    {
        return true;
    }
    else
    {
        return false;
    }
}